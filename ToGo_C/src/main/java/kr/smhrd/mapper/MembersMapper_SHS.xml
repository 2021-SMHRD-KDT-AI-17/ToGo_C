<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.smhrd.mapper.MembersMapper_SHS">

	<select id="memberslogin" parameterType="kr.smhrd.entity.Members" resultType="kr.smhrd.entity.Members">
		select * from members where mb_id=#{mb_id} and mb_pw = #{mb_pw}
	</select>
	
	<update id="memberUpdate" parameterType="kr.smhrd.entity.Members">
		UPDATE members SET mb_pw=#{mb_pw}, mb_nick=#{mb_nick}, mb_phone=#{mb_phone} where mb_id=#{mb_id}
	</update>
</mapper>